# layero-sample-monorepo

Fixture for the setup wizard's monorepo folder picker. The repo root has
**no** build manifest — the actual app lives in `apps/web/` (Vite). Used to
verify auto-suggest ("приложение в подпапке") + per-subdir detection.
